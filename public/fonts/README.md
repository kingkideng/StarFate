Place `LXGWWenKaiLite-Regular.ttf` in this directory to self-host LXGW WenKai Lite.

The stylesheet already prefers this file when it exists and falls back to local
system installs plus platform Chinese serif fonts. Runtime CDN font imports were
removed so users in mainland China are not blocked by external font hosts.
